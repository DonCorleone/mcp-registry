apiVersion: v1
kind: Service
metadata:
  name: mcp-registry
  namespace: mcp-registry  # Anpassen falls anderer Namespace
spec:
  selector:
    app: mcp-registry
  ports:
    - port: 8080
      targetPort: 8080
